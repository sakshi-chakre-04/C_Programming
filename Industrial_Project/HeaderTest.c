#include<stdio.h>   // this line is replaced here with prototypes

// entry point function
#define MAX 100 // this line is removed

int main()
{
    int no = MAX + 11;  // this line has its value
    printf("Value of no is %d\n ", no);

    //the whitespaces are mostly removed


    

    return 0;
}

// -save-temps
// the .i file has more bytes 